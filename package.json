{
  "name": "electron-test",
  "version": "1.0.0",
  "description": "**Clone and run for a quick way to see Electron in action.**",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ayush-lal/Test.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ayush-lal/Test/issues"
  },
  "homepage": "https://github.com/ayush-lal/Test#readme",
  "devDependencies": {
    "electron": "^11.0.2",
    "electron-builder": "^22.9.1",
    "build": "electron-builder build --mac --win --publish never",
    "deploy": "electron-builder build --mac --win --publish always"
  },
  "dependencies": {
    "electron-updater": "^4.3.5"
  }
}
